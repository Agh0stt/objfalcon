import "std"

// Recursive function — factorial.
int fact(int n) {
    return n <= 1 ? 1 : n * fact(n - 1);
}

int main() {
    print(fact(1));    // 1
    print(fact(5));    // 120
    print(fact(10));   // 3628800
    return 0;
}
