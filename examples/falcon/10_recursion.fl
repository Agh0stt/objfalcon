import "std"

# Recursive function — factorial.
func fact(n: int) -> int {
    if (n <= 1) {
        return 1
    }
    return n * fact(n - 1)
}

func main() -> void {
    print(fact(1))    # 1
    print(fact(5))    # 120
    print(fact(10))   # 3628800
}
