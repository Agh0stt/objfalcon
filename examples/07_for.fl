import "std"

# for loop — sum 1..10.
# Syntax: for (init, cond, post) { body }
func main() -> void {
    sum: int = 0
    for (i: int = 1, i <= 10, i += 1) {
        sum += i
    }
    print(sum)   # 55
}
