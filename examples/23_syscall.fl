import "std"

# __syscall — raw Linux int 0x80 call.
# sys_write(fd=1, buf, len) = syscall 4.
func main() -> void {
    msg: str = "direct syscall\n"
    __syscall(4, 1, msg, _flr_str_len(msg))   # sys_write stdout
}
