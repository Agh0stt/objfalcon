import "std"

// for loop — sum 1..10.
// ObjFalcon for: for (init; cond; post) or for (init, cond, post)
int main() {
    int sum = 0;
    for (int i = 1; i <= 10; i += 1) {
        sum += i;
    }
    print(sum);   // 55
    return 0;
}
